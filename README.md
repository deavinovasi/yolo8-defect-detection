# yolo8-defect-detection
YOLO-based defect detection project for image and video inference.  The system processes video input, performs object detection, and converts the output video to MP4 (H.264) for browser compatibility.
